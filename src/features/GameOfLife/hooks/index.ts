// Hook to manage grid state, including next generation calculation and toggling cells
export { useGridState } from "./useGridState";

// Hook to handle canvas drawing and interaction
export { useCanvas } from "./useCanvas";

// Hook to manage game controls (play, pause, speed, etc.)
export { useGameControls } from "./useGameControls";
